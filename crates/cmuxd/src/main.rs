//! `cmuxd` — long-lived daemon owning every `claude` PTY.
//!
//! Phase 1: stub. Real socket + session ownership lands in phase 2/3.
//! See `DAEMON_PLAN.md`.

fn main() {
    eprintln!("cmuxd: not implemented yet — see DAEMON_PLAN.md");
    std::process::exit(2);
}
